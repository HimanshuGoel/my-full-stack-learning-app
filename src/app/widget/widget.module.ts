import { NgModule } from '@angular/core';

@NgModule({
  // Use a widget NgModule to make a component, directive,
  // or pipe available to external NgModules. Import widget NgModules
  // into any NgModules that need the widgets in their templates.
  // Many third-party UI component libraries are provided as widget NgModules.
  declarations: [],
  exports: []
})
export class WidgetModule {}
